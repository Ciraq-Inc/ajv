services:
  app:
    build: ./
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 4000
      NITRO_PORT: 4000
      NITRO_HOST: 0.0.0.0
      NUXT_PUBLIC_API_BASE: https://devapi.medsgh.com
    ports:
      - "4010:4000"
